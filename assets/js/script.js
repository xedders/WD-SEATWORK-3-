function openGallery(section) {
    if (section === 'header') {
        window.location.href = './header-gallery/index.html'; // Navigate to the index.html in header-gallery folder
    } else if (section === 'footer') {
        window.location.href = './footer-gallery/index.html'; // Navigate to the index.html in footer-gallery folder
    }
}
